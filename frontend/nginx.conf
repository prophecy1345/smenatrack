server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: любой маршрут отдаёт index.html, иначе перезагрузка /login даёт 404
  location / {
    try_files $uri $uri/ /index.html;
  }
}
